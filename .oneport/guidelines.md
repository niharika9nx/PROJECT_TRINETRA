# Team guidelines

Rules every Oneport tool enforces (review, secrets, migrate, …). Add lines like:

- never commit real secrets, even in tests — use placeholders
- all public functions need type hints
